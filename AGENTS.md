
## Admin tasks requiring screenshot
- For admin tasks that require a screenshot, ALWAYS:
  1) wait for the password prompt,
  2) enter the password,
  3) wait until validation succeeds,
  4) only then take the screenshot.
- Never take the screenshot before the authentication flow is fully completed.
